# KATA TDD CON PYTEST

## PYTEST

Crea el entorno

```
python3 -m venv venv
```

Activalo (comando linux)

```
source venv/bin/activate
```

Instala pytest

```
(venv) pip3 install -U pytest
(venv) pytest --version
```
